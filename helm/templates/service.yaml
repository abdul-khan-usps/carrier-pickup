apiVersion: v1
kind: Service
metadata:
  name: {{ include "carrier-pickup.fullname" . }}
  labels:
    {{- include "carrier-pickup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: http
  selector:
    {{- include "carrier-pickup.selectorLabels" . | nindent 4 }}
